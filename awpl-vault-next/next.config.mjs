/** @type {import('next').NextConfig} */
const nextConfig = {
  // If adding to your existing Vercel site as /vault route,
  // you can copy the app/vault/ folder into your main project.
  // If deploying as standalone, no changes needed.
}
export default nextConfig
